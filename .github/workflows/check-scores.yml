name: Update Match Results - TennTrend

on:
  # Run twice daily:
  # 1. At 08:00 CET (07:00 UTC) - Morning check for overnight matches
  # 2. At 22:00 CET (21:00 UTC) - Evening check for day matches
  schedule:
    - cron: '0 7 * * *'   # 07:00 UTC = 08:00 CET / 09:00 CEST
    - cron: '0 21 * * *'  # 21:00 UTC = 22:00 CET / 23:00 CEST
  
  # Allow manual trigger for testing
  workflow_dispatch:

# Grant write permissions for GitHub Actions
permissions:
  contents: write

jobs:
  update-results:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Update match results (HYBRID: Odds API + SofaScore)
        env:
          THE_ODDS_API_KEY: ${{ secrets.THE_ODDS_API_KEY }}
          TZ: Europe/Stockholm
        run: npm run update-results
      
      - name: Commit and push updated history
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "TennTrend Bot"
          git add public/data/results-history.json
          git diff --staged --quiet || git commit -m "chore: auto-update match results $(date +'%Y-%m-%d %H:%M CET')"
          git push
      
      - name: Notify on failure
        if: failure()
        run: |
          echo "⚠️ Results update workflow failed. Check logs for details."
